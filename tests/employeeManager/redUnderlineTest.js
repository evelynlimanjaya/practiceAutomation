module.exports={
    before:browser=>{
        browser.url('https://devmountain-qa.github.io/employee-manager/1.2_Version/index.html')
            .waitForElementVisible('body')
    },
    after:browser=>{
        browser.end()
    },
    'Red underline test':browser=>{
        browser
            .click('li[name="addEmployee"]')
            .click('li[name="employee11"]')
            .click('input[name="nameEntry"]')
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .click('#saveBtn')
            .verify.cssClassPresent('input[name="nameEntry"]','materialInput invalidInfo')
            .setValue('input[name="nameEntry"]','John Doe')
            .click('input[name="phoneEntry"]')
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .click('#saveBtn')
            .verify.cssClassPresent('input[name="phoneEntry"]','materialInput invalidInfo')
            .setValue('input[name="phoneEntry"]','1234567890')
            .click('input[name="titleEntry"]')
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .keys(browser.Keys.BACK_SPACE)
            .click('#saveBtn')
            .verify.cssClassPresent('input[name="titleEntry"]','materialInput invalidInfo')
    }
}